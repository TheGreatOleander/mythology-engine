# Usage

## Full cycle

```bash
python runtime/run_studio.py --mode manual --action full-cycle
```

## Release package only

```bash
python runtime/run_studio.py --mode assisted --action release-package
```

## Status mode

```bash
python runtime/run_studio.py --mode manual --action status
```
